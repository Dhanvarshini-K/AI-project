export interface SocialIconProps {
    src: string;
    alt: string;
}

export interface NavLinkProps {
    label: string;
}

export interface ContactInfoProps {
    website: string;
    email: string;
    mobile: string;
}